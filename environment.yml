channels:
  - conda-forge
dependencies:
  - jupyter-server-proxy>=1.4
  - pip
  - websockify
  - paramiko                # adiciona o 
  - pip:
      - MetaTrader5
      - jupyter-keepalive>=0.1.1
      
